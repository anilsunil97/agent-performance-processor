🚀 AGENT PERFORMANCE PROCESSOR - QUICK COMMANDS REFERENCE
========================================================

📦 ESSENTIAL COMMANDS FOR THIS PROJECT:

1. INSTALL DEPENDENCIES:
   pip install streamlit pandas openpyxl pyinstaller pillow

2. RUN STREAMLIT APP:
   streamlit run streamlit_app.py

3. RUN NATIVE GUI:
   python agent_performance_gui.py

4. BUILD EXECUTABLE:
   pyinstaller native_gui.spec --clean

5. INSTALL NSIS (for installer):
   winget install NSIS.NSIS

6. BUILD WINDOWS INSTALLER:
   & "C:\Program Files (x86)\NSIS\makensis.exe" installer.nsi

7. CREATE HD ICONS:
   python create_hd_icon.py

8. GIT OPERATIONS:
   git add .
   git commit -m "Your message"
   git push origin main

9. CHECK PROJECT STATUS:
   git status
   Get-ChildItem | Format-Table Name, Length

10. CLEAN BUILD FILES:
    Remove-Item "build" -Recurse -Force
    Remove-Item "dist" -Recurse -Force

📁 KEY FILES CREATED:
- streamlit_app.py (Web application)
- agent_performance_gui.py (Native GUI)
- native_gui.spec (PyInstaller config)
- installer.nsi (NSIS installer script)
- app_icon_hd.ico (HD Windows icon)
- AgentPerformanceProcessor_Setup.exe (Windows installer)
- AgentPerformanceProcessor_Offline.exe (Portable executable)

🎯 FINAL RESULT:
✅ Web app: https://agent-performance-proceappr-arjunexpress.streamlit.app/
✅ GitHub: https://github.com/anilsunil97/agent-performance-processor
✅ Windows installer with HD icons and full integration
✅ Portable executable for immediate use
✅ Professional documentation and guides

ALL COMMANDS EXECUTED SUCCESSFULLY! 🎉